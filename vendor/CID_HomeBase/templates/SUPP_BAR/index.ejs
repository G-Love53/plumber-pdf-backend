<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= ((typeof templateName !== "undefined" && templateName) ? templateName : "CID Document") %></title>

  <!-- Inline CSS injected by svg-engine -->
  <style>
    <%- (typeof styles !== "undefined" ? styles : "") %>
  </style>
</head>
<body>

<% (pages || []).forEach((svg, idx) => { %>
  <div class="page">
    <%- svg %>
  </div>

  <% if (idx < pages.length - 1) { %>
    <div class="page-break"></div>
  <% } %>
<% }); %>

</body>
</html>
